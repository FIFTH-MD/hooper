name: Autodeploy

on:
  push:
    branches:
      - test

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: yarnpkg/setup-node@v4

      - name: Install dependencies
        run: yarn install
